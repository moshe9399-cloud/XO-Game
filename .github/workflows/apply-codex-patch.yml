name: Apply Codex XO patch

'on':
  push:
    branches:
      - main
    paths:
      - '.github/workflows/apply-codex-patch.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - name: Verify workflow can run
        run: echo "Codex workflow is running"
